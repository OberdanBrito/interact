import { defineConfig } from "vite";

export default defineConfig({
  // Configurações específicas (proxy de API, aliases) serão adicionadas
  // conforme a integração com o backend for definida.
});
