{
  "name": "interact-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend da plataforma Interact — API REST + MongoDB",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "node src/db/seed.js",
    "db:reset": "docker compose down -v && docker compose up -d && sleep 5 && npm run seed"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0"
  }
}
